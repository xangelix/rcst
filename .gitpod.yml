image:
  file: .gitpod.Dockerfile

vscode:
  extensions:
    - dart-code.dart-code
    - dart-code.flutter
    - nash.awesome-flutter-snippets
    - ms-azuretools.vscode-docker
    - valentjn.vscode-ltex

tasks:
  - command: |
      chmod +x ./.gitpod.init_signing.sh
      bash ./.gitpod.init_signing.sh